const showError = (message = "Error") => {
  console.error(message);

  // get element that will display the error message
  // .innerText in that element = message

  // check if error message is NOT displayed -> show it
};

const clearMessages = () => {
  // if message error is displayed -> hide it
  // get element for success message -> toggle it
};
